<main class="contenedor contenedor-formularios">
  <h1><%= nombrePagina %></h1>
  <form class="default-form" method="post" enctype="multipart/form-data">
    <div class="campo">
      <label for="imagen">Imagen</label>
      <input type="file" name="imagen">
    </div>
    <% if(grupo.imagen) { %>
      <div class="campo">
        <label for="imagenActual">Imagen Actual</label>
        <img src="/uploads/grupos/<%= grupo.imagen %>" alt="Imagen Actual" width="400">
      </div>
    <% } %>
    <div class="campo enviar">
      <input type="submit" value="Subir Imagen" class="btn btn-rosa">
    </div>
  </form>
</main>